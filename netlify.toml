[build]
  command = "npm ci && npm run build"
  publish = "public"

[build.environment]
  NODE_ENV = "production"

[context.production.environment]
  PHP_VERSION = "8.3"

[dev]
  command = "npm run dev"
  port = 5173
